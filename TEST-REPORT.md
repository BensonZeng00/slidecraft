# 0.4.0-beta.8 忠实复刻与 CLI 保真约束

日期：2026-09-19。针对“复刻时擅自纠正文案、改连线和简化装饰”的实际问题更新。此次只修改技能和检查工具，没有重做旧 PPT，也没有调用生图服务。

- **通过：96 项标准库回归测试**，保留原有 68 项，新增 28 项保真检查。覆盖未授权的文字、字体大小、布局、背景、装饰、插画细节、箭头路线和端点变化；明确且精确的用户修改范围；过期源图/清单/计划；缺失原图对照；额外或重复原生文字；零高度水平连接线；旧制作入口不能绕过保真检查。
- **通过：技能格式校验与文档相对链接检查**。已移除“用生成前文案覆盖已确认图”“制作时自动纠正错误箭头”等冲突指令。
- **通过：真实旧产物的负向检查**。本次 AI 发展史 PPT 的旧记录虽有原生文本、PowerPoint 渲染和一般视觉检查，但没有冻结的原图清单及对照记录，新 `check_delivery.py --state` 正确返回 `draft`。未修改旧产物或补造通过证据。
- **接口变化**：新增 `workflow.py fidelity-check`；`reconstruct-check` 和 `validate_state.py --allow-produce` 同时检查原图清单与复刻计划；`check_delivery.py` 新增必需的 `--state` 参数。旧项目须先从真实原图补清单，再实际对照渲染，不需要为未变化的原图重复索要确认。
- **边界**：CLI 比较结构化声明、文件哈希和原生文字，不自动理解图片、不认证用户授权、不保证清单没有遗漏。几何、插画、纹理等实际保真仍需原图与导出 PPT 逐项目视对照。新版生成/复刻行为尚未做新的端到端实测，不能把合成测试说成“再也不会改图”。

---

# 0.4.0-beta.7 按阶段加载优化

日期：2026-09-18。仅调整文档路由与必读范围，CLI 实现及门禁不变；未进行新的生图或 PPT 生产。

- 主文件变成阶段路由，新增 `design-stage.md` 和 `production-stage.md`。普通单页第一阶段不再强制预读后端选择、重建、预检与交付资料。多页说明仍按需加载。
- 68 项原有 CLI 回归测试通过；技能格式与相对链接检查通过。脚本与 beta.6 字节一致，未弱化确认、预算或交付检查。
- `o200k_base` 对文档原文逐文件计数：主文件 1,575 → 482 token；普通单页设计必读合计 7,881 → 1,323 token，下降约 83.2%。旧口径为主文件＋design-and-cli＋runtime-adaptation＋prompt-to-infographic＋project-state；新口径为主文件＋design-stage。
- 这是文档加载预算，不是整次任务的实际 token 节省。未包含宿主提示、消息封装、图像工具技能、推理、工具输出、图片理解、缓存或重读；异常恢复、多页及复杂提示词会按需增加读取。
- 阶段 2 在确认后读取 475-token 阶段入口，再依执行步骤加载原有制作与验收文档。不是删除质量规则或保证全过程 token 减少 83.2%。
- 路由效果为静态审查与本地计数；新会话模型是否始终遵循最小读取集合、新版真实用户确认流程仍待实测。

---

# 0.4.0-beta.6 增量测试记录

日期：2026-09-18。环境：Codex / Windows，共享 Python；本次未调用生图服务。

- **通过：68 项 CLI/标准库回归测试**。保留 49 项旧测试，新增 19 项设计哈希确认、恢复门禁、路径限制、素材提取预算、文字实测尺寸预检及 CLI 退出码测试。
- **通过：静态技能校验与相对链接检查**。技能版本、README、单页与多页说明同步改为设计图确认后才制作 PPT。
- **边界：合成测试不等于人类确认实测**。新版真实“生图 → 用户确认 → PPT”双阶段流程、完整三页流程和其他平台仍待实测。案例 03/04 是此前产物，不补造确认记录。
- **边界：CLI 不生成 PPT、不自动测量字体**。复用平台 PPT 技能的构建/字体测量/渲染能力；新预检只检查传入的实测几何，不能代替视觉检查。生图与第三方渲染不保证字节级确定性，未测量 token 节省比例。
- **边界：确认记录仅检查一致性**。图像或冻结内容文件改动会阻止复刻；实际用户意图、确认来源和是否展示过图片仍须由执行者核实。

运行：`python -m unittest discover -s scripts -p "test_*.py"`。

---

# 0.4.0-beta.5 增量测试记录

日期：2026-09-18。环境：Codex / Windows，用户配置的共享 Python。状态：内部候选版。

本次新增标准库渲染器只读探测、交付证据一致性检查和场景图片映射检查；保留后端无关架构。补充实际生图工具/提供方记录要求。未调用任何生图服务或自动安装渲染器。

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| 脚本回归 | 49 项测试通过 | 原有 36 项，加 13 项交付/探测合成测试 |
| 本机渲染器探测 | 通过 | 从两个 COM 注册视图与实际程序路径检测到 PowerPoint，正确标记 detected_not_tested；未启动应用 |
| WorkBuddy 实际失败案例 | 通过（正确拒绝完成状态） | 当前 PPTX 为 50 个形状、1 张 SVG 图片；缺少渲染记录且 scene 未声明该图片，返回 draft 和原因 |
| 技能格式、相对文档链接、ZIP 字节比对 | 通过 | 仅包级验证 |
| 新版 WorkBuddy 完整制作与真实渲染 | 待验证 | 没有把脚本测试当成跨宿主端到端通过 |

交付检查只验证证据记录的完整性与一致性，不实际看图、不认证记录真实性、不替用户批准页面。下面保留旧版证据与限制。

---

# 0.4.0-beta.4 增量测试记录

日期：2026-09-17。环境：Codex / Windows，用户配置的共享 Python。状态：内部候选版。

本次改动：明确 PPT 技能/插件选择优先级；增加前置能力检查、有限恢复、素材兼容保真与文件哈希验收规则；增加标准库 PPTX 只读检查 CLI。未绑定或安装任何 PPT 后端。

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| 状态与审批预算回归 | 27 项合成测试通过 | 复用现有状态代码，没有伪造真实审批 |
| 新 PPTX 检查 CLI | 9 项合成测试通过 | 覆盖实际幻灯片顺序、拆分文字 run、段落边界、页数不匹配、缺失资源、SVG 警告、过期哈希、输入保护和错误输入 |
| 本地已有 WorkBuddy PPTX | 结构检查通过 | 实际读取 1 页、90 个形状、0 图片；未在本轮重新生图、渲染或验证全部内容清单 |
| 技能格式与文档链接 | 通过 | 不等于其他宿主端到端通过 |
| 新版 WorkBuddy 端到端执行 | 待验证 | 对话记录是旧版案例，不能用来证明本版适配完成 |
| 素材转换、真实渲染与审阅 | 待验证 | 本次增加工作流约束，未声称实现转换器或渲染器 |

新 CLI 只做结构检查和文件变更检测。视觉检查永远不能由对象计数或渲染命令退出码代替。原有真实应用测试保留如下，不追认成本次新测。

---

# 0.4.0-beta.3 测试记录

日期：2026-09-15。状态：内部测试候选版，不是“全部平台通过”的正式版。

本版新增“先讨论并确认大纲及每页内容，再开始逐页制作”的计划审阅关卡，同步状态记录和校验脚本。以下生图/PPT/PowerPoint 实测证据来自 beta.1；beta.3 重新运行状态测试和包校验，不声称重新执行了生图或多页测试。已有视觉优先约定及许可文件保持不变。

## 本轮实际执行

| 场景 | 结果 | 证据与边界 |
| --- | --- | --- |
| 中文提示词生成信息图 | 通过 | “幻灯匠的制作流程”，实际调用内置生图 1 次，得到 1 张参考图，没有重试，实际费用不可得 |
| 信息图重建原生 PPTX | 通过 | 复用 Codex 已有 Presentations 技能，运行时版本 26.909.22227；15 个含原生文字的形状，3 条附着连接线，没有嵌入整页图片 |
| 最终文件检查 | 通过 | 导出后重导入并渲染，检查文字覆盖、页面比例与原生对象；首版箭头反向经目视检查发现，修正后再次导出和渲染 |
| PowerPoint 实际编辑 | 通过 | Windows PowerPoint 16.0，通过 COM 打开测试副本，替换较长中文段落、移动节点、另存重开；文字及位置保留，3 条线仍附着，另存后的预览已目视检查 |
| 当前平台已有 PPT 工具 | 通过 | 直接复用，没有安装重复技能，没有启用通用 PPT 库后备路线 |
| 状态与预算脚本 | 27 项合成测试通过 | 在原有 19 项基础上，增加未确认大纲阻止第 1 页生图及重建、大纲批准只放行第 1 页、每页内容或版本变化使旧大纲批准失效、逐页内容/顺序校验、旧状态不自动批准和恢复待确认大纲；不代替大模型行为测试 |
| 包结构与引用 | 通过 | 技能格式校验及相对文档链接检查通过，UI 元数据合法；只读工具使用标准库，不包含 PPT 生成后端 |

PowerPoint 第一次在受限会话中启动失败（0x80070520）；授权环境复核成功。该失败是本机会话权限问题，不是平台 PPT 插件安装失败案例。

首版连接线端点参数与语义方向不一致，结构检查未发现。已将“核对渲染后的真实箭头方向”纳入通用重建规范，而不是硬编码某一个后端的参数。

## 尚未通过的发布关卡

- 三页真实逐页审阅：旧测试保留已有第 1 页样例，第 2、3 页未开始。恢复测试时需先向用户讨论并确认大纲及每页内容，不把旧样例或之前的制作请求视为大纲批准。新增计划关卡及完整改稿、确认、跨会话恢复、最终合并尚待真实测试。脚本中的模拟批准不是用户批准。
- Claude Code、WorkBuddy 及其他宿主：未在相应运行环境实际安装或执行。千问/豆包的技能导入与可调用 PPT 能力也未验证。
- PPT 技能缺失、安装成功、安装失败/待登录：验收方案已建立，本轮没有破坏现有环境来制造这些场景，也没有声称已验证。
- 复杂插画隔离：旧版图片重建案例存在裁剪接缝和简化光效；新版已增加检查要求，但本轮采用纯原生流程图，没有复杂插画可用于证明这一项已解决。
- 许可选择已完成：本版按所有者要求加入 PolyForm Noncommercial 1.0.0 原文、规定声明和商业授权说明。正式发布页面仍需明确实际权利人及商业联系渠道；本次不代为创建仓库、发布或发送商业授权。

## 样例复现与私有证据

单页样例输入：制作一张“幻灯匠的制作流程”信息图，依次展示内容策划、信息图参考、可编辑重建、逐页确认。暖白背景、青色节点、橙色最终节点，中文、横版。每个节点有简短说明，底部说明复杂插画可保留为独立图片。

样例文件独立于技能包保留：`slidecraft-single-v2.pptx`、`single-reference.png`、`single-powerpoint-preview.png`。项目内另存原始生图提示词、场景描述、导出检查回执、原生覆盖报告、PowerPoint 编辑副本及进度记录。构建代码依赖本机已安装的专有平台能力，不随技能包分发。

本记录不证明所有输入、字体、复杂图表或其他演示软件都兼容；任何新后端按 [发布验收](references/release-checks.md) 补充证据后再提升兼容级别。
